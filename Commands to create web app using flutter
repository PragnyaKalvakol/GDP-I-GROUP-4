To create a new app that includes web support

flutter create loow_admin
cd loow_admin

To use the latest version of the Flutter SDK:

flutter channel stable
flutter upgrade


To serve your app from localhost in Chrome

flutter run -d chrome

command to generate a release build

flutter build web


